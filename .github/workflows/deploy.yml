name: Update and deploy Docker images

on:
  push:
    branches:
      - main

jobs:
  build-nginx:
    uses: netwerk-digitaal-erfgoed/workflows/.github/workflows/deploy.yml@c030912d5f7a7bc7334379e186f9affea1d5d7c7
    with:
      target: nginx
    secrets:
      CONTAINER_REGISTRY_TOKEN: ${{ secrets.CONTAINER_REGISTRY_TOKEN }}

  build-php-fpm:
    uses: netwerk-digitaal-erfgoed/workflows/.github/workflows/deploy.yml@c030912d5f7a7bc7334379e186f9affea1d5d7c7
    with:
      target: php-fpm
    secrets:
      CONTAINER_REGISTRY_TOKEN: ${{ secrets.CONTAINER_REGISTRY_TOKEN }}
